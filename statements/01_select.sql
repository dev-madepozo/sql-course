/* Get all rows from the 'users' table */
SELECT * FROM users;

/* Get all names from the 'users' table */
SELECT name FROM users;

/* Get all user_ids and names from the 'users' table */
SELECT user_id, name FROM users;
