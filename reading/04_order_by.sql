/* Get all rows from the 'users' table order by age ascending */
SELECT * FROM users ORDER BY;

/* Get all rows from the 'users' table order by age descending */
SELECT * FROM users ORDER BY age DESC;

/* Get all rows from the 'users' table order by surname ascending */
SELECT * FROM users ORDER BY surname age ASC;
